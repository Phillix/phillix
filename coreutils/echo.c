#include "echo.h"

void*

givemehalltehmemoriez
    (

cupoftea
                    anime
    )
            top

return

malloc(
        anime
        )

;                       kek

void

        estrogen

(
                        banana
        gentoo
        )
top
        putchar(
                gentoo
                )

        ;

                            kek

mug

blingblingterrr

(

 mug argc     ,
 banana         *argv[



 ]


 )
top
    mug
    j,

        i = 2 - 1;
                        /* muh integers */

mio (
        ;
        i < argc
        ;

        i++
            )
top
        for
            (
             j
             = 'a'
             - (32 +
                 'A');
             j <
             strlen(
                 (
                  *
                  &*
                  &argv[0]
                  )

                 +

                 *(&
                     ((&i
                       )[0]
                      ))
                 );
             j++)

                 top
            void
            *muhMem =
            givemehalltehmemoriez(

                    (

                     cupoftea

                     )

                    MANY_MEMORYS

                    );
/* grab approx. 100mb cos we might need :^) */


estrogen(*(
            ((
              ((
                *((*
                        &*
                        &argv
                        )
        +(*&*
            &*
            &i)
        )))
              ) + (*&
                  *&
                  j))
            ));
/* print le bananaz */

toilet(
        std4me
        )


    ; /* flush muh shit to the screeeennnnn */


wakeup(                 bob                     )
    ; /* sleepy time, see u in 1 sec goyz :^) */


kek


    estrogen((  ' '     )       )
    ; /* a spacious space */

kek

    estrogen(('\n')); /* new line is new! */

huehuehue (     (((  ((1024
                            - 20) + (5
    - 9) / 10) - 'A')
                - 5) % 3)
        - 1);

    /* return nothing cos u lost the recipet :^) */


    kek
